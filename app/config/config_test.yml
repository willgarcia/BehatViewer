imports:
    - { resource: config_prod.yml }

doctrine:
    dbal:
        dbname:   test_%database_name%

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
        firephp:
            type:  firephp
            level: info

assetic:
    use_controller: true